services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: legal_source_ai
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - legal_source_ai_data:/var/lib/postgresql/data

volumes:
  legal_source_ai_data:
